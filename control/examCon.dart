import '../models/Course.dart';
import 'CorurseCon.dart';

class ExamControler {
//  double totalMark({required String name}){
//   List<Corurse> listCorurse = CorurseCon.listCorurse;
//      int index = listCorurse.indexWhere((element) => element.name == name);
//     if (index != -1)
//       {
//        return listCorurse[index].listExam.map((e) => e.mark).toList().reduce((value, element) => element+value);
//       }
//     else{
//       print("Not found");
//       return 0.0;
//     }
      
//   }

}